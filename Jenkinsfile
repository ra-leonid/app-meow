pipeline {

    agent any

    stages {
        
        stage('Build docker image') {
            steps {
                sh """
                echo "Success build docker image"
                """
            }
        }
    }   
}