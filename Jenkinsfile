pipeline {

    agent {
        node {
            label 'jenkins-jenkins-agent'
        }
    }

    stages {
        
        stage('Build docker image') {
            steps {
                sh """
                echo "Success build docker image 1"
                """
            }
        }
    }   
}